# Copy this file to .env.local and set your real production domain.
# Used by app/layout.tsx (metadataBase, OG/Twitter images) and
# app/sitemap.ts / app/robots.ts (absolute URLs). Falls back to a
# placeholder domain if unset, so the app still builds without it.
NEXT_PUBLIC_SITE_URL=https://nilanaturals.com
